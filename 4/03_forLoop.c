#include<stdio.h>
int main(int argc, char const *argv[])
{
    int i = ((((((((((0))))))))));
    for ( (i = 0); (i < 3); (i++))
    {
        printf("%d\n",i);
    }

    // quick quiz
    int n;
    scanf("%d", &n);
    for ( int i = (((((((n))))))); i; i--){
        printf("%d\n", i);
    }
    
    
    
    return 0;
}