#include<stdio.h>
#include<string.h>
int main(int argc, char const *argv[]){
    
    // char *text = "This is me here in my computer and i like all marvel characters and the best one is Caption America(Chris Evans). i like him i love him more than anyone else. I also like other movies character like Eleven(Millie Bobby Brown) from Stranger Things and Spider-Man(Andrew Garfield) from The Amazing Spider Man and also Gwen Stacy(Emma Stones) from the same movie The amazing Spider Man";
     
    // int length = strlen(text);
    // printf("The length of the string is %d\n", length); 

    // char *st1 = "Tanmay";
    // char st2[40];
    // strcpy(st2,st1);
    // printf("Now the str2 is not str1 %s", st2);

    // char st1[45] = "Tanmay ";
    // char *st2 = "Kumar";
    // strcat(st1,st2);
    // printf("%s", st1);


    // char st1[45] = "Tanmay";
    // char *st2 = "Kumar";
    // int val = strcmp(st1,st2);
    // printf("%d", val);

    // strlen(string);
    // strcpy(string1, string2);
    // strcat(string1, string2);
    // strcmp(string1, string2);

    return 0;
}